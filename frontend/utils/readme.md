What this module does is hold utility functions and global constants used throughout the app.

Development Roadmap:

Create formatCurrency(), parseDate(), and getCategoryColor() helpers.
Add constants like category names, leaderboard logic, and color themes.
Write validation utilities for inputs (e.g., validateBudgetAmount()).
Create theme-related files (e.g., colors.js, spacing.js).