This module manages the app’s navigation system, such as tabs, stacks, or drawer layouts.

Development Roadmap:

Set up React Navigation with @react-navigation/native and stack, tab, or drawer navigators.
Create a MainNavigator.js that connects all main screens.
Configure App.js to wrap navigation in NavigationContainer.
Add deep linking support (optional for push notifications).