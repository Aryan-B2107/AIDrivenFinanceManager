What this module does is store static files like images, icons, fonts, and illustrations used throughout the UI.

Development Roadmap:
Add icons (wallet, trophy, chatbot, etc.) for each feature.
Include placeholder avatars or illustrations for gamification or onboarding.
Import custom fonts and set them globally using expo-font or react-native-config.